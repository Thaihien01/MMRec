embedding_size: 64
feat_embed_dim: 64

n_layers: [2, 3]
dropout: [0.2, 0.3]
reg_weight: [0.001, 0.01]
cl_weight: [0.05, 0.1, 0.2]

predictor_in_eval: True
eval_normalize: True

use_neg_sampling: False

gated_fusion: True
multimodal_gcn: True
mlp_predictor: True
separate_predictors: True
mlp_features: True

hyper_parameters: ["n_layers", "cl_weight", "reg_weight", "dropout"]

epochs: 600