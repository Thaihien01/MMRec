embedding_size: 128
feat_embed_dim: 128
weight_size: [128, 128]

learning_rate: 0.001
lambda_coeff: 0.9
reg_weight: [0.0001]
reg_weight_l2: [0.00001]

n_mm_layers: 1
n_ui_layers: [3]
knn_k: 10

mm_image_weight: 0.3
dropout: [0.3]
mm_edge_dropout: [0.1]

mlp_features: True
gated_fusion: True

hyper_parameters: ["n_ui_layers", "reg_weight", "reg_weight_l2", "dropout", "mm_edge_dropout"]

epochs: 600