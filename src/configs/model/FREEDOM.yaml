embedding_size: 128
feat_embed_dim: 128
weight_size: [128, 128]

learning_rate: 0.001
lambda_coeff: 0.9
reg_weight: 0.0001

n_mm_layers: 1
n_ui_layers: 3
knn_k: 10

mm_image_weight: 0.3
dropout: 0.3

hyper_parameters: []

epochs: 600